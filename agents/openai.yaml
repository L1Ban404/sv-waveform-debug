interface:
  display_name: "SystemVerilog Waveform Debug"
  short_description: "Investigate RTL failures from FST/VCD evidence"
  default_prompt: "Use $sv-waveform-debug to investigate this hardware failure from waveform evidence and RTL source."
